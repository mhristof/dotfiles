[core]
	excludesfile = ~/.gitignore_global
[alias]
	undo = reset --soft HEAD~1
	squash-all = "!git reset --soft $(git merge-base master $(git rev-parse --abbrev-ref HEAD)) && git commit -am \"$(git rev-parse --abbrev-ref HEAD)\" && git rebase master"

[include]
    path = ~/.gitconfig_local
