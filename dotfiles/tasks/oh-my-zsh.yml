---

- name: clone OMZ repo
  git:
    repo: https://github.com/robbyrussell/oh-my-zsh.git
    dest: '{{ home }}/.oh-my-zsh'

- name: set user shell
  become: true
  user:
    name: '{{ user }}'
    shell: /bin/zsh
