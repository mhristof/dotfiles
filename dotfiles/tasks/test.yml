---

- meta: flush_handlers

- name: check available commands
  shell: source ~/.zshrc && which {{ item }}
  environment:
    DOTFILES_SKIP_SSH_ADD: 1
  changed_when: false
  args:
    executable: /bin/zsh
  with_items:
    - autojump
    - fzf
    - vim
    - pip
  tags:
    - test
