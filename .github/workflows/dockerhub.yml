name: dockerhub

on:
  - push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: build
        run: make hub
      - name: docker login
        run: docker login --username = ${{ secrets.DOCKER_TOKEN }}
      - name: push
        run: make push
